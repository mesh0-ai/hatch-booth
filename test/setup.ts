/**
 * `toBeInTheDocument` and friends. Hatch's own frontend asserts on returned
 * nodes instead and needs no setup file; this app leans on the matchers.
 */
import "@testing-library/jest-dom/vitest";
